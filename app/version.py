"""
Service version constant for the Signing Service.

This is the single source of truth for the service version. Import this
constant wherever the version string is needed (health endpoints, tests, etc.)
to ensure there is no drift between reported and tested values.
"""

VERSION = '2.0.0-rc1'
